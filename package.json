{
  "name": "codepresso-plugin",
  "version": "0.1.1",
  "description": "Team workflow plugin - GitHub PR logging + Notion sync for Claude Code",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test tests/lib/*.test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.994.0",
    "@modelcontextprotocol/sdk": "^1.25.3"
  }
}
