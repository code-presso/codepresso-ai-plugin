---
description: Configure Codepresso for this machine or project
argument-hint: [optional setup notes]
---

# Codepresso Setup

The user invoked this command with: $ARGUMENTS

Read `skills/setup/SKILL.md` and follow its setup workflow. Verify prerequisites, inspect existing
`~/.codepresso/config.json` and `.codepresso.json`, and only write configuration after confirming
the values with the user.
