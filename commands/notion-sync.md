---
description: Sync or inspect Codepresso Notion tasks
argument-hint: [task query or update request]
---

# Codepresso Notion Sync

The user invoked this command with: $ARGUMENTS

Read `skills/notion-sync/SKILL.md` and use the Codepresso Notion MCP tools when available. Do not
call the Notion API directly unless the skill explicitly requires a fallback.
